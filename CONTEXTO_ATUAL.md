# Contexto Atual - Estoque App

## Objetivo
- Controlar fluxo de itens unitários por QR entre indústria e lojas.
- Garantir rastreabilidade completa do item no ciclo: origem -> trânsito -> destino.

## Visão de produto (north star)
- **Missão:** dar à rede o melhor caminho para o produto **entrar certo** na matriz e **seguir a rota até a filial**, com rastreio forte e operação simples na loja.
- **Origem:** sistema criado para necessidade real da empresa; evoluir para **SaaS** e outros segmentos (ex.: farmácias e varejo afim), sem prender o desenho a um único tipo de loja.
- **Venda na loja (direção):** cliente conclui compra com foco em **QR**; o funcionário atua como **conferente** (confirma que está pago / pode entregar), reduzindo gargalo de pagamento e dependência de PDV mal integrado.
- **Fiscal (direção):** entrada de **nota fiscal por imagem** → checagem de qualidade → extração estruturada dos dados → persistência no servidor → uso consistente para **obrigações e planejamento tributário** (pagamento do mínimo legalmente devido com base em dados corretos).
- **Roadmap de intenção (não prioritização técnica):** (1) consolidar logística matriz–filial e QR operacional; (2) camada de venda/checkout por QR + papel de conferência na loja; (3) pipeline fiscal digital (captura → validação → armazenamento → relatórios).

## Perfis e escopo
- `OPERATOR_WAREHOUSE`: operação de indústria.
- `MANAGER`: visão operacional/gerencial.
- `OPERATOR_STORE`: operação da loja vinculada em `local_padrao_id`.
- `DRIVER` / `OPERATOR_WAREHOUSE_DRIVER`: transporte e viagem. Tela **Viagem / Aceite** (`/viagem-aceite`): mostra origem, totais, lojas de destino; **Ver produtos por remessa** com resumo por produto e expansão para lista unitária (QR/token curto) por transferência. Após aceitar/iniciar: **`refetch` imediato** + lista `viagens` com `preserveDataWhileRefetching` (sem tela inteira em loading); um fluxo **Aceitar e iniciar agora** (pendente) com confirmação no **Modal** do app; viagem só **aceita** (legado) ainda mostra **Iniciar viagem** com o mesmo modal; erros em faixa vermelha no app (sem `alert` do browser). `iniciarViagem` atualiza remessas em **paralelo**; mutações com checagem de `error` do Supabase.

## Usuários operacionais
- Tela `/login` **não** exibe lista de usuários/senhas. **Autenticação:** só `POST /api/auth/operacional` com **bcrypt** em `credenciais_login_operacional` + `login_operacional` em `usuarios` (ativo). Sem hash no banco → mensagem pedindo configuração em **Cadastros → Usuários**. **Railway / `.env.local`:** `SUPABASE_SERVICE_ROLE_KEY` obrigatória. **Cadastros → Usuários** (`ADMIN_MASTER`): define/remove credencial (hash). **Carga inicial opcional:** `npm run seed:operacional` + `scripts/operacional-seed.local.json` (gitignored; modelo `scripts/operacional-seed.example.json`).
- Equipe típica: Leonardo (indústria), Ludmilla (gerente), Joana / Simone / Luciene / Francisca / Júlia / Lara / Silvania (lojas conforme `local_padrao_id`), Marco (admin). Telefones e lojas devem bater com **Locais** e `usuarios` no Supabase; operador de loja sem loja resolvida não consegue fluxo completo.

## Fluxo oficial de transferência
- `AWAITING_ACCEPT` -> `ACCEPTED` -> `IN_TRANSIT` -> `DELIVERED` (ou `DIVERGENCE`).
- Recebimento só lista transferências `IN_TRANSIT` para a loja do usuário (`OPERATOR_STORE`).

## Regras importantes ativas
- Operador de loja vê somente dados da própria loja em recebimento/aceites/estoque/validades.
- **Validades** (`/validades`): consulta no Supabase **filtrada** (não carrega a tabela inteira de `itens`). **Loja** = `local_padrao_id` da operadora; **indústria** (`OPERATOR_WAREHOUSE` / `OPERATOR_WAREHOUSE_DRIVER`) = mesmo critério no local da matriz; **gerente/dono** = todos os locais, lista ordenada por unidade. Ignora datas sentinela (sem vencimento). Atualização periódica (~90 s) e botão **Atualizar**.
- **Alertas de validade (global):** `ValidadeAlertProvider` no `AuthGuard` — aviso **3 dias** + vencidos, escopo igual à tela Validades (loja / matriz / rede). **Banner** fino sob o header: crítico (vencidos) ou atenção (a vencer), link **Ver validades**, **Fechar** (sessionStorage por assinatura de contagem; **ícone Timer** no header mantém **badge** com total). Home sem bloco extra de validade. Refetch ao focar a aba.
- Tela **Estoque** (`OPERATOR_STORE`): consulta ao resumo SQL **só** com `local_id = local_padrao_id` da loja; **não** há seletor “Todos os locais” nem RPC com `p_local_id` nulo (evita vazar indústria/consolidado). Escopo da loja usa `usuario` do hook **ou**, na primeira pintura, `getUsuarioLogado()` — evita chamar a RPC antes do `useAuth` hidratar (corrida que mostrava consolidado e sumia ao trocar filtro de estado). Respostas assíncronas defasadas são ignoradas (gerador de fetch). Sem `local_padrao_id`, lista vazia + aviso para cadastrar loja e relogar.
- Sessão de usuário é revalidada com o banco ao carregar o app (corrige `local_padrao_id` desatualizado no navegador).
- Login operacional de `OPERATOR_STORE` não reutiliza `local_padrao_id` antigo quando a loja padrão não é resolvida.
- Atualização de estoque agregado usa `upsert` com conflito em `produto_id` (evita erro de chave única duplicada).
- Estoque ganhou modo gerencial "Visão do dono" (ADMIN_MASTER/MANAGER) com consolidado por unidade (lojas + indústria), totais por local e distribuição por produto.
- Baixa diária possui proteção contra leituras duplicadas da câmera e passa a resolver código escaneado por token completo/curto.
- Baixa diária: leitor de QR **desligado** até o usuário tocar em **Ativar leitor de QR (câmera)**; digitação manual continua sob demanda (botão).
- Padrão de UX de QR: **sem** abrir câmera automaticamente; botão para ativar o leitor + digitação manual sob demanda em recebimento, separar por loja, `/qrcode` e rastreio por QR.
- Textos de UX/erro de QR padronizados nas telas operacionais (mensagens de não encontrado, falha de busca e placeholders de digitação manual).
- Ajustes mobile-first aplicados em telas administrativas com formulários/tabelas (quebra responsiva de grids, cabeçalhos e scroll horizontal controlado em tabelas largas).
- Registrar Compra suporta lançamento por `Unidade`, `Caixa` e `Fardo`, com conversão para **quantidade no lote** (sem gerar QR na entrada). Validade gravada em `lotes_compra.data_validade`. QR emitidos na **separação** ou na **produção** (FIFO de lotes). Migração `20260408100000_compra_sem_qr_resumo_estoque.sql` (RPC `resumo_estoque_agrupado` / `resumo_estoque_minimo` + coluna `data_validade`). **Unidade de rastreio:** cada unidade no lote pode gerar um QR; embalagem com muitas peças e **só um QR na caixa** → quantidade no lote = número de **caixas** (Caixa/Fardo com «unidades rastreáveis por embalagem» **1**, ou modo Unidade por caixa). Ao trocar de **Unidade** para **Caixa/Fardo**, o app zera o fator para **1** (evita erro operacional). Aviso na tela se o fator por embalagem for **> 50**. Cadastro de produto: texto de ajuda no estoque mínimo. Legado SQL: `docs/consultas-sql/caixa-unidade-rastreio-legado.sql`. **Correção aplicada (2026-04-09):** potes Galvanotek + Porta talher consolidados para **27 / 19 / 9** unidades rastreáveis (caixas), custos por caixa nos produtos/lotes; registro `docs/consultas-sql/correcao-galvanotek-porta-talher-2026-04-09.sql`.
- **Corrigir entrada de compra** (`/entrada-compra`): bloco **Corrigir lançamentos recentes** (últimos lotes) com **Editar** — ajusta quantidade (unidades no lote), custo unitário, fornecedor, NF / sem NF e validade. Regra: `quantidade` do lote **não pode ser menor** que o número de linhas em `itens` com `lote_compra_id` (QR já emitidos). Serviço `atualizarLoteCompra` + auditoria `ALTERAR_LOTE_COMPRA`; recalcula `estoque` agregado do produto.
- Registrar Compra permite edição rápida do produto selecionado sem sair da tela (nome, unidade, fornecedor, estoque mínimo e custo de referência).
- Validade em compra é opcional para produto sem regra de vencimento no cadastro (validade zerada), e obrigatória apenas para produto perecível.
- Em recebimento:
  - lista de itens esperados;
  - marcação pendente/escaneado;
  - bloqueio de QR fora da transferência;
  - proteção contra scan duplicado;
  - confirmação da transferência grava `transferencia_itens` e `itens` em **lote** (poucas idas ao Supabase) e sincroniza `estoque` por produto em paralelo.
- Confirmação (`window.confirm`) nos principais botões operacionais.
- Sincronização de estoque agregado (`estoque`) via recálculo por produto nos fluxos de despacho, recebimento, entrada de compra, produção, baixa diária e descarte.
- **Produção** (`/producao`): operador informa **produto acabado**, **baldes** (cada balde = 1 QR do acabado no local), **local** warehouse, **validade** e **insumos gastos** (produto de compra/AMBOS + quantidade de unidades QR). Sistema baixa insumos por FEFO (`created_at` ASC), grava `producoes` com `local_id`/`num_baldes`, `baixas.producao_id` e `producao_consumo_itens`, depois gera itens/etiquetas do acabado. Migração: `20260407183000_producao_consumo_insumos.sql`. Modal de confirmação **só fecha após sucesso**; erros exibidos no próprio modal (incl. `details`/`hint` PostgREST); checklist âmbar quando o botão está desabilitado; auditoria de baixas em lotes de 80.
- Reconciliação SQL do agregado `estoque` com base em `itens` (`estado = EM_ESTOQUE`) executada para eliminar divergências históricas de quantidade.
- Hook de consulta em tempo real (`useRealtimeQuery`) passou a paginar automaticamente para não truncar tabelas grandes (evita sumiço de produtos na tela de estoque quando há muitos itens).
- Tela de estoque otimizada para performance: filtros de `estado` e `local` são aplicados na consulta Supabase (server-side), com payload reduzido e paginação maior por lote.
- Paginação do `useRealtimeQuery` foi estabilizada para bases grandes com `count exact` + páginas de até 1000 registros, evitando truncamento silencioso por limite da API.
- Tela `Estoque` usa ordenação estável por `id` na consulta paginada para não perder/duplicar itens entre páginas.
- `useRealtimeQuery` ganhou deduplicação de fetch em voo + paralelismo controlado de páginas (batch) para reduzir tempo de carregamento inicial em tabelas volumosas.
- Tela `Estoque` passou a usar resumo agregado no banco (`resumo_estoque_agrupado`) em vez de carregar todos os itens no front, mantendo busca/filtros e reduzindo drasticamente o payload inicial.
- Tela `Estoque` ganhou aba `Estoque mínimo`, baseada em função SQL agregada (`resumo_estoque_minimo`), com foco em itens abaixo do mínimo para apoiar compra/reposição.
- A aba `Estoque mínimo` considera produtos ativos com `estoque_minimo > 0`, incluindo cenários de saldo zero por local (faltante calculado no banco).
- **Família do produto** (antiga “categoria” de negócio): tabela `familias`, coluna `produtos.familia_id`. Cadastro em `Cadastros -> Categorias` (CRUD em `familias`; exclusão bloqueada se houver produtos).
- **Tipo de embalagem**: tabela legada `grupos` + vínculo `produto_grupos` (somente embalagem). Cadastro em `Cadastros -> Tipos de embalagem` (CRUD em `grupos`; exclusão bloqueada se houver `produto_grupos`).
- Migração `20260402140000_familias_grupos_embalagem_canonica.sql`: cria `familias`, preenche `familia_id` a partir de vínculos antigos de família em `produto_grupos`, unifica `tipos_embalagem` em `grupos`, move `embalagem_tipo_id` para `produto_grupos`, remove `tipos_embalagem` e coluna `embalagem_tipo_id`.
- `Registrar Compra` (modal rápido): família em `familias` (criar/editar no modal); tipo de embalagem em `grupos` (criar/editar no modal); produto grava `familia_id` e até um `grupo_id` em `produto_grupos`.
- `ProdutoModal` e lista de produtos: família + embalagem separados conforme modelo acima (indústria exige família).
- **Cadastros → Produtos** (`/cadastros/produtos`): lista carregada por `fetchProdutosCadastroLista` (uma query PostgREST com embeds `familia`, `produto_grupos`/`grupos`, `conservacoes`; fallback em lotes se o embed falhar). Atualização em tempo real (canal com debounce) em `produtos`, `produto_grupos`, `conservacoes`, `familias`. Filtro de status na tabela (todos / ativos / inativos); evita o padrão N+1 do `transform` antigo em `useRealtimeQuery`.
- **Reposição de estoque por loja** (`Cadastros`): elegíveis = **COMPRA** (sempre); **AMBOS** só com `escopo_reposicao = loja` (cadastro fornecedor); **PRODUCAO** **nunca** entra (indústria). `escopo industria` exclui sempre. Por loja: coluna **Na loja** grava `loja_produtos_config.ativo_na_loja`; só `ativo_na_loja` entra em **Separar por Loja** (reposição), **contagem na loja** e no resumo de faltantes (`getResumoReposicaoLoja` / `participaReposicaoLoja`). Filtro opcional “Ocultar inativos”. **Salvar** em lote (mínimo + ativo) com `confirm`. Migrações: `20260402150000` etc.
- **Declarar estoque na loja** (`/contagem-loja`, operador de loja): mesma regra de elegibilidade + **`ativo_na_loja`**; `ensureTodosProdutosElegiveisNaLoja` ao carregar. **UI só para o funcionário:** produto + **quantidade que tem**. Grava `loja_contagens`.
- **Ciclo operacional do QR (matriz → loja):** o cadastro de **produto** não implica etiqueta física; unidades entram com token em **compra/produção**. Na prática, **etiquetas com QR** são geradas na **separação para a loja** (impressão 60×30), coladas no pacote e enviadas; na loja o recebimento lê esse QR. Scanner/digitação em separação manual serve sobretudo quando a unidade **já** tem QR legível (ex.: reimpressão, conferência).
- `Separar por Loja` — **modo reposição:** ao definir origem e destino, carrega faltantes e **aplica sugestão** automaticamente (debounce ~450 ms; troca de loja/indústria refaz o fluxo). Botão **Recarregar faltantes e sugestão** força nova leitura. **Modo manual:** tabela **Total** + **Com QR** + **Livre** (total menos o já na lista); botão **+** usa FEFO em `itens` e, se faltar, **`emitirUnidadesCompraFifo`** (auditoria `EMITIR_UNIDADES_COMPRA`). Por padrão **oculta** produtos com `origem === COMPRA` (polpas/insumos de fornecedor); checkbox **«Mostrar também produtos só de compra»** quando for enviar mercadoria comprada. **Opcional** leitor QR / digitação de token (oculto na reposição). Controle de concorrência por epoch evita estado inconsistente ao trocar selects rápido. **Painel «Envios já registrados»:** após escolher a **origem (indústria)**, lista transferências **WAREHOUSE_STORE** (filtro opcional destino): data, status, totais, resumo por produto, lote `SEP-…`, link **Etiquetas**; em **Aguardando aceite** / **Aceita**, **Editar destino** (outra loja) e **Excluir remessa** (remove transferência + vínculos, etiquetas do lote `excluida`, viagem órfã; itens permanecem na origem).
- **Cadastros — Indústria** (`/cadastros/industria`): resumo do **dia** (`loja_produtos_config`, produtos novos elegíveis, lojas STORE criadas) com filtro opcional por loja; links **Editar** conforme permissão (`reposicao-loja`, `produtos`, `locais`). Rota liberada a `OPERATOR_WAREHOUSE` e `OPERATOR_WAREHOUSE_DRIVER` (quem não tem cadastro vê «Sem permissão» nos edits).
- No modo reposição de `Separar por Loja`, a lista exibe apenas produtos com faltante (`mínimo_loja > contagem`), reduzindo ruído operacional.
- `Separar por Loja`: **fluxo recomendado** — **Criar separação** e, em seguida, imprimir quando o sistema perguntar (snapshot dos mesmos `item_id` da transferência, lote `SEP-{viagem}`). Após cada separação criada, o app guarda em **localStorage** a **última remessa** (lote, loja, lista de unidades): painel **Imprimir pedido completo** no topo da tela com **Imprimir remessa inteira na Zebra (Pi)** (vários envios sequenciais ao Pi se a remessa for grande) ou **Remessa inteira no navegador** (janela única); persiste ao fechar a aba (migra dados legados de `sessionStorage` v1). Texto de ajuda quando ainda não há remessa salva; scroll suave até o painel após **Criar separação**. **Guia PDF** e **Só imprimir** antes da separação exigem `confirm` explicando risco de QR recusado no recebimento se a lista divergir. **Gravação `etiquetas`:** id = id do item; impressão pós-separação = `impresso_agora` + lote `SEP-…`; impressão antecipada = lote `SEPARACAO-LOJA`; ao criar viagem antes (upsert `manter_impressa_se_existir`) lote `SEP-…` sem zerar `impressa` se já true. Validade ausente: sentinela `2999-12-31`. Limpeza em massa de `etiquetas` no Supabase **não remove** `itens`.
- **Guia PDF + etiquetas** em `Separar por Loja`: PDF + janela de impressão em **60×30** (fluxo operacional); texto de confirmação alinhado a esse formato.
- **Recebimento:** se o QR resolve um item fora de `transferencia_itens`, mensagem orienta conferir remessa e alinhamento com a separação registrada.
- Tela **Etiquetas** (`/etiquetas`): select de remessas `SEP-…` lista **transferências** `WAREHOUSE_STORE` com `viagem_id` (igual «Envios já registrados»), **sem** exigir linha em `etiquetas` para aparecer no dropdown; operador indústria filtra por `origem_id = local_padrao_id`; completa com **RPC** `etiquetas_lotes_sep_recentes` (lotes distintos; migração `20260409120000`) ou fallback limitado em `etiquetas`; teto **200** opções. Meta origem/destino por viagem: consulta a `transferencias` em **fatias** de `viagem_id` (evita URL/query gigante). `useRealtimeQuery` com `maxRows` pagina em **paralelo** (até 4 faixas) para carregar a remessa mais rápido. Recarrega opções quando o usuário hidrata. O operador **escolhe o lote** e só então carrega as etiquetas daquela remessa (teto por remessa, `useRealtimeQuery` com `enabled` no lote). Lista: **sem** embed `itens!…` no PostgREST (evita falha se o nome da FK divergir); tokens vêm de consulta em lote a `itens`. Para lote `SEP-…`, painel âmbar com **Gravar etiquetas a partir da transferência** fica **sempre** visível (também quando já há linhas, para recompor a lista). Chama `POST /api/operacional/sync-etiquetas-remessa` (login/senha operacional + **service role** grava `etiquetas`). `sincronizarEtiquetasRemessaPorLoteSep`: junta **todos** os `item_id` das transferências `WAREHOUSE_STORE` da mesma `viagem_id` com o **mesmo** `destino_id` (erro se houver destinos diferentes); deduplica QR. `criarTransferencia` valida itens e grava `transferencia_itens` em **fatias** (evita query-string enorme com centenas de UUID); em falha no vínculo remove a `transferencias` criada. Rejeita **o mesmo** `item_id` repetido na lista. **Envios já registrados:** contagem por **item distinto** (linha duplicada em `transferencia_itens` não infla «unidades»). Migração **`20260408203000_transferencia_itens_unique_item.sql`**: remove duplicatas e índice único `(transferencia_id, item_id)`. **Validação login** compartilhada: `operacional-auth-server.ts` + rota `POST /api/auth/operacional`. Por grupo, **50** linhas na DOM até **expandir** (performance). Painel data/ora, origem → destino, `transferencias`; impressão **Zebra/Pi** e **remessa inteira no navegador** seguem o **formato do seletor** (`localStorage`): **60×30** → ponte Pi **estoque**; **60×60** → ponte **indústria**, com **fallback** para **estoque** se indústria não estiver configurada (um único Pi; aviso âmbar na tela); remessas `SEP-…` preenchem **nome da loja de destino** nas etiquetas. **Padrão** de formato na tela **60×60** (adesivo quadrado); com **SEP-…** e **60×30** o app mostra **aviso** (PDF é folha **60×30** com duas meias — não preenche adesivo 60×60 inteiro). `ultima-remessa-storage`. **Impressão Pi** por pendentes, grupo e linha. Serviço: `etiquetas-opcoes-remessa.ts`.
- **Pi — remessas grandes:** impressão na estação usa **`enviarEtiquetasParaPiEmMultiplosJobs`** (~40 etiquetas por envio WebSocket, jobs nomeados `… 1/N`, timeout proporcional, pausa curta entre jobs) em **Separar por Loja**, **Etiquetas** e **Produção**; evita JSON/HTML único gigante e sobrecarga no Chromium/CUPS. Em **`label-print`**, QRs (`toDataURL`) são gerados em **lotes paralelos** para reduzir pico de memória no celular/notebook.
- Impressão de etiquetas (`label-print`): **Separar por Loja** e **60×30** na tela **Etiquetas**: duas meias-etiquetas por folha (**N unidades ⇒ ⌈N/2⌉ folhas** na Zebra; não confundir folhas com unidades); **BALDE Nº** no **rodapé** (acima de Val./Op.), tipografia e QR (~11,8 mm) ajustados à meia-etiqueta 15×30 mm. **60×60:** **BALDE Nº** em faixa no **topo** (abaixo da loja). **Lote `SEP-…`:** `numero_sequencia_loja` = **1..N por remessa** (baldes ordenados por `item_id`), recalculado com **todas** as etiquetas ativas do lote + o payload do upsert — **não** depende de `destino_id` na hora da impressão (evita 1,1,2,2 quando a meta da transferência atrasava e o front pulava o upsert). Várias `transferencias` na mesma `viagem_id`: meta na tela Etiquetas usa a **mais recente** (`WAREHOUSE_STORE`). **Lote `SEPARACAO-LOJA`:** sequência **global** por loja (`reservar_sequencia_balde_loja`). Regra balde: nome com **«balde»**. Em **`/etiquetas`**, antes de imprimir, o app **grava** números via upsert + `destino_id` da transferência. Rodapé 60×30: **Val. dd/mm/aa** ou **Imp.** + **Op.** (truncagem curta). **`/etiquetas`:** `data_validade` da linha `etiquetas` vai para impressão sempre que existir (antes dependia de regra de perecível no cadastro do produto). **Última remessa** em `localStorage` guarda `destinoLocalId` para reimpressão manter o contexto da sequência. Migração **`20260408133000_sequencia_balde_loja_destino.sql`**. **Produção** (indústria) usa **60×60** (`FORMATO_ETIQUETA_INDUSTRIA`): ordem de leitura **de cima para baixo** — produto, **loja** (`nomeLoja`), **BALDE Nº**, **RESFRIADO**; **validade** (dd/mm/aa) + **Gerou** (`responsavel`) ao lado do **QR** (loja **não** repetida nessa coluna); tokens e **lote exibido curto** em lotes `SEP-{uuid}` (primeiro segmento após `SEP-`); **rodapé legal** (empresa/CNPJ/endereço) **no fundo**; navegador ou **Zebra / Pi** (`industria`). **Sem** ler `localStorage` nesses fluxos. **Etiquetas** (tela) segue o seletor + `localStorage`; Pi **60×60** tenta indústria e cai em estoque se necessário. **Zebra / CUPS 60×60 no Pi:** configurar fila com **mídia padrão 60×60 mm** — `docs/CUPS_ZEBRA_60X60.md` + script `scripts/pi-print-ws/cups-adicionar-fila-60x60.sh`; também `docs/IMPRESSAO_TERMICA_ZEBRA.md` §5 e `docs/RASPBERRY_INDUSTRIA_NOVO_PI.md`. **Mesmo Raspberry** que a ponte estoque: manter **`ZebraZD220`** (60×30) para separação e fila **`ZebraZD220-6060`** (Custom.60x60mm) para 60×60; no Supabase **`cups_queue`** da linha **`industria`** = fila 60×60 (e URL/token alinhados à estoque) para o app não enviar a fila 60×30 em jobs 60×60.
- **Raspberry Pi (rede local, usuário `kim`, SSH `kim@192.168.1.159`):** serviço **systemd** `pi-print-ws` em `~/pi-print-ws` — WebSocket **TCP 8765**, HTML → PDF (Chromium / **puppeteer-core**, `preferCSSPageSize`) → **`lp`** (CUPS). O servidor usa **`widthMm`/`heightMm`** do JSON (via `formatoEtiquetaPdf` no app) para **`setViewport`** antes do render — necessário para **60×60** ocupar a etiqueta inteira (evita miniatura no canto). **Fallback** no Pi (JSON sem dimensões): **60×60 mm** (`PRINT_DEFAULT_WIDTH_MM` / `PRINT_DEFAULT_HEIGHT_MM` no `.env` para sobrescrever; Pi só de separação física 60×30 pode usar `PRINT_DEFAULT_HEIGHT_MM=30`). Atualize `server.mjs` no Pi ao mudar esse fluxo. `.env` no Pi: `PRINT_WS_TOKEN`, **`CUPS_QUEUE=ZebraZD220`**. **Zebra ZD220** USB. **Túnel Cloudflare nomeado** **`pi-print-acaidokim`** → **`print.acaidokim.com.br`** → `http://127.0.0.1:8765`; conector **`cloudflared.service`** (oficial). A unit **`cloudflared-pi-print-ws`** (quick + **`cloudflared-quick-tunnel-sync.sh`** / RPC) ficou **desativada** para evitar conflito. **DNS:** CNAME **`print`** → **`3406c32f-fd37-4dff-9da8-41e88cef2976.cfargotunnel.com`** (proxied), se o painel não criar sozinho. **App / Supabase:** `wss://print.acaidokim.com.br` em **`config_impressao_pi`** e/ou **`NEXT_PUBLIC_PI_PRINT_WS_URL_ESTOQUE`** no Railway. Segundo Pi: **`PI_TUNNEL_PAPEL=industria`**. Docs: **`docs/TUNEL_PERMANENTE_PRINT_PI.md`**, `docs/IMPRESSAO_PI_ACESSO_REMOTO.md`, **`docs/RASPBERRY_INDUSTRIA_NOVO_PI.md`**. Migração **`20260406120000_config_impressao_pi_papel.sql`**.
- **“Offline / fetch failed” em Verificar agora:** não é teste da USB da Zebra — é o **servidor do app** chamando `GET https://…/health` no host efetivo do túnel (**Supabase** ou variável **`NEXT_PUBLIC_PI_PRINT_WS_URL_*`** no deploy). **`ENOTFOUND …trycloudflare.com`** com URL só no banco = quick tunnel antigo; mitigar com **env estável** (túnel nomeado), sync no Pi ou atualizar **`ws_public_url`**. **`ENOTFOUND`** a um hostname válido em DNS público (ex. `print…`) pode ser **resolver DNS do processo** (Railway, macOS/VPN): a rota `/api/impressoras/status` tenta **`dns.setServers`** (1.1.1.1 / 8.8.8.8 ou **`PI_PRINT_STATUS_DNS_SERVERS`**) e, se ainda falhar, **DNS-over-HTTPS** (1.1.1.1) + HTTPS ao IPv4 da edge com **SNI** correto. Tela **Impressoras (Pi)** avisa se o Supabase tem `trycloudflare` sem override por env. Deploy Railway: ver `docs/FLUXO_ENTREGA.md`.
- Rota **`/teste-impressao-etiqueta`**: amostra fictícia; **`?papel=industria`** testa a segunda ponte; padrão **estoque**. Permissões iguais a **Etiquetas**.
- **Hospedagem:** app Next.js em **Railway** com build **Railpack** (sem Docker no repo). Variáveis `NEXT_PUBLIC_SUPABASE_*` no build e runtime; **`SUPABASE_SERVICE_ROLE_KEY` no runtime** (login operacional). Um único gatilho por mudança: push **ou** `npm run railway:release` — não os dois em sequência. Vários **`QUEUED`**: **`RAILWAY_PROJECT_TOKEN`** (token do projeto) ou **`RAILWAY_TOKEN`** (conta) + **`npm run railway:prune-queued`** (ver `docs/FLUXO_ENTREGA.md`); não commitar token. Diagnóstico: **`npm run railway:diagnose`**; deploys Docker presos: cancelar no dashboard.
- **Fluxo de entrega:** ver `docs/FLUXO_ENTREGA.md`. **GitHub Actions** (`CI`) em push/PR para `main` executa `npm ci` + **`npm run lint`** + `npm run build` (env Supabase fictícia no runner). Node **20** (`.nvmrc`). Template de PR com checklist.
- **Supabase (um banco):** app e MCP usam o mesmo projeto; `npm run sync:mcp-supabase` alinha `~/.cursor/supabase-mcp.env` ao `.env.local` (reiniciar MCP no Cursor). `npm run env:supabase-ref` mostra o ref. Doc: `docs/SUPABASE_AMBIENTE_E_MCP.md`; SQL: `docs/consultas-sql/estoque-por-loja.sql`.

## Situação validada recente
- Migração `20260408100000_compra_sem_qr_resumo_estoque.sql` aplicada no Supabase ligado ao MCP/`.env.local` (coluna `lotes_compra.data_validade` + RPCs de resumo). Se outro projeto Supabase ainda falhar com a mesma mensagem, aplicar o mesmo SQL lá.
- Transferência para Loja Paraiso foi recebida e concluída com itens movidos para destino.
- Joana está vinculada no banco à Loja Paraiso.
- Após deploy/migração: aplicar `20260402140000_familias_grupos_embalagem_canonica.sql` no Supabase se o projeto ainda não tiver família/`grupos` canônicos. **`config_impressao_pi` com `papel`:** migração `20260406120000` (duas linhas; segredo de sync distinto por Pi).
